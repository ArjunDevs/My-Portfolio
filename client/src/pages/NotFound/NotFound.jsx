import React from "react";
import { PageTransition } from "../../components/PageTransition/PageTransition";

const NotFound = () => {
    return (
        <div>
            <h1>where you going</h1>
        </div>
    );
};

export default PageTransition(NotFound);
